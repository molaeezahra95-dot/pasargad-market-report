# وضعیت پروژه: pasargad-market-report

## شروع
- اجرا: `RUN.bat`
- بررسی بدون اجرای شبکه یا ساخت خروجی: `RUN.bat --check`
- خروجی اصلی: `index.html`
- نوع: `static`

## کدهای فعال
- `index.html`

## دسته‌بندی
- `_support/docs`: مستندات، راهنماهای قدیمی و شرح نیازها
- `_support/inputs`: فایل‌های ورودی و مرجع منتقل‌شده از ریشه
- `_support/launchers`: راه‌اندازهای قدیمی یا حالت‌های جایگزین؛ اجرای اصلی فقط RUN.bat است
- `_support/records`: گزارش‌ها و فهرست‌های انتقال
- `data` یا `Data` و `private`: داده‌های اجرایی؛ محتویات و حساب‌ها حذف نشده‌اند
- `config`: تنظیمات؛ `tests`: آزمون‌ها؛ `output`: خروجی‌های تاریخی
- فایل‌های requirements و pyproject/package در ریشه، تنظیمات ضروری نصب و ابزار توسعه‌اند.

## تحویل کار به جلسهٔ بعد
آخرین اقدام: یکدست‌سازی اجرا و دسته‌بندی فایل‌ها در ۲۰۲۶/۰۹/۱۵.
وضعیت اجرای واقعی: نتیجهٔ بررسی در CHECK-RESULT.txt ثبت می‌شود؛ بررسی اولیه جای آزمون کامل داده‌ها نیست.
موضوع نیمه‌تمام قبلی: اتصال داشبوردهای پرتال به نسخه‌های گیت‌هاب هنوز کامل نشده است.
قبل از ادامه، تغییرات گیت و فایل‌های زیر را بررسی کنید؛ تاریخ فایل به‌تنهایی ملاک نسخهٔ بهتر نیست.

```text

```

## بررسی تکمیلی ۲۰۲۶/۰۹/۱۵
بررسی وجود کد و وابستگی‌ها موفق؛ اجرای کامل هنوز آزمایش نشده است.
فایل‌های تغییرکرده در این مرحله: `RUN.bat`، `_support/start.py`، `_support/project.json`، `_support/CHECK-RESULT.txt` و همین فایل وضعیت.
گام بعد: آزمایش قابلیت‌های برنامه با داده واقعی؛ اتصال پرتال به نسخه‌های گیت‌هاب همچنان نیمه‌تمام است. تغییرات این مرتب‌سازی هنوز پوش نشده‌اند.

## Publication verified 2026-09-16
Branch: `sync/organized-local-20260916`
Published commit: `5994284061196ba62f2ebae2805b435865441ead`
Remote SHA matched local commit. Main and existing remote versions preserved.
